# Copy to .env or export these before running.
# Required: your Anthropic API key (the SDK also honors an `ant auth login` profile).
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides:
# MACRO_MODEL=claude-opus-5   # estimation engine model
# MACRO_DB=macro.db           # SQLite file path
